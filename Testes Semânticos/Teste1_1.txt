% Após na Linha 13 era esperado um ; ou um END %
routine
declare
 int a, b;
 int resul;
 float a, x;
begin
 a := 12;
 x := 12;
 read (a);
 read (b);
 read (c)
 result := (a*b + 1) / (c+2);
 write "Resultado: ";
 write (result);
end